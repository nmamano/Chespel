config {
    centipawn_value = 32;
}

num prova() { return 2; }

void cosa() { }

rule checkCoses {
    num x, y = 20, z = y;
    string s = "patates";
    string p = s ++ " i dracs";
    [string] vec = [];
    vec = vec ++ [p];
    len(vec);
    prova();
    num p;
    p = cosa() + 2;
    x = 4;
    score x+y+z+vec.len;
}
