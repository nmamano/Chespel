config {
    centipawn_value = 32;
}

rule checkCoses {
    num x, y = 20, z = y;
    string s = "patates";
    string p = s ++ " i dracs";
    [string] vec = [];
    vec = vec ++ [p];
    len(vec);
    num p;
    x = 4;
    score x+y+z+vec.len;
}
